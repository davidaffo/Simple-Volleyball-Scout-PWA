{
  "name": "simple-volleyball-scout-pwa",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:web": "rm -rf www && mkdir -p www && cp -R index.html style.css js icons manifest.json service-worker.js www/"
  }
}
